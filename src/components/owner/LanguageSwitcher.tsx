/**
 * Перемикач мови вимкнено: інтерфейс — лише українською.
 * Компонент лишено як no-op, щоб не ламати імпорти у вже побудованих сторінках.
 */
export function LanguageSwitcher() {
  return null;
}
